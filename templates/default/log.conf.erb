log {
  source(s_<%= @app.name %>);
  destination(d_<%= @app.name %>);
};

log {
  <% for @source in @app.source %>
    source("<%= @source %>");
  <% end %>
  <% for @destination in @app.destination %>
    destination("<%= @destination %>");
  <% end %>
}
