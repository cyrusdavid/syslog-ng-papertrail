destination d_<%= @app.name %> {
  <% unless @app.udp.nil? %>
    udp("<%= @app.udp.host %>"
        port(<%= @app.udp.port %>)
      <%= ", template(t_#{@app.template}" if @app.template %>));
  <% end %>
};
